CREATE OR REPLACE PROCEDURE 'IRI_DATA_PRC'
(
p_out_err_code OUT NUMBER,
p_out_err_msg OUT VARCHAR2
)
AS 
CURSOR IRI_DATA 
IS
SELECT /*+PARALLEL(4)*/
ID FROM PARTIES WHERE 
ID NOT IN (SELECT PARTY_ID FROM PARTY_ADDRESSES);
TYPE MASTERIRI_DATA_T
IS
TABLE OF IRI_DATE%ROWTYPE;
IRI_DATA_CUR MASTERIRI_DATA_T;
l_num NUMBER;
l_err_msg VARCHAR2(5000);
l_limit pls_INTEGER:=5000;
l_end_date VARCHAR2(50):='31-DEC-9999';
BEGIN
OPEN IRI_DATA;
LOOP
FETCH IRI_DATA BULK COLLECT INTO
IRI_DATA_CUR LIMIT l_limit;

FORALL I IN 1... IRI_DATA_CUR.COUNT

INSERT INTO  

END IRI_DATA_PRC;